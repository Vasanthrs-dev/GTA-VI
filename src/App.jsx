import gsap from "gsap";
import { ScrollTrigger } from "gsap/all";
gsap.registerPlugin(ScrollTrigger);

function App() {
  return (
    <mail>
      <div className="text-red-600">GTA 6</div>
    </mail>
  );
}

export default App;
